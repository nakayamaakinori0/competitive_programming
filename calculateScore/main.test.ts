import { main } from "./main";

describe("test of main", () => {
  test("test1", () => {
    const input = ["helloWorld"];
    const result = main(input);
    expect(result).toBe();
  });

  test("test2", () => {
    const input = ["helloWorld"];
    const result = main(input);
    expect(result).toBe();
  });

  test("test3", () => {
    const input = ["helloWorld"];
    const result = main(input);
    expect(result).toBe();
  });
});
